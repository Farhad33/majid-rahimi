'use client'
import styled from 'styled-components'

export default function Header({ Component, pageProps }) {
    return (
        <Work>Work in progress...</Work>
    )
}

const Work = styled.h1`
    text-align: center;
    font-size: 40px;
    color: red;
    margin: 20px;
`